# Step 1 フレーム抽出GUI

Step 1 は、360°動画からSfM/3DGSに使うエクイレクタングラー静止画を作る画面です。ここで作った `images/` と `selected_frames.csv` が、Step 2の確認、Step 3のマスク生成、Metashape SfMへ進む土台になります。

多くの場合は、動画を選び、シーンフォルダを指定し、まず固定間隔で抽出します。必要に応じて `変化補正` を使うと、似すぎたフレームを減らし、視点変化が大きい区間の候補を追加できます。

## 起動

```bat
run_gui.bat --scene .\scene01
```

起動後、ワークフロー左側の `Step 1: フレーム抽出` を開きます。

## まず決めること

| やりたいこと | 推奨設定 |
| --- | --- |
| まずMetashape用のフレームを普通に作りたい | `固定間隔 1.0秒`, `変化補正 ON` |
| とにかく早くフレームだけ出したい | `クイック抽出 ON` |
| 歩き撮りや室内で近距離物体が多い | `シーン距離: 近距離・歩行` |
| 空撮、広場、海岸、山など遠景が多い | `シーン距離: 遠景・空撮` |
| 同じ動画を設定を変えて作り直したい | `画像保存方法: リセットして上書き` |
| 複数動画を同じシーンに追加したい | `画像保存方法: 新規のみ追加` |

シーンフォルダのパスに日本語などの非ASCII文字、極端に長いパス、制御文字や `"` が含まれる場合、GUIは実行前に止めます。外部ツールで失敗しやすいため、英数字だけの短い作業パスを使ってください。

## 基本操作

1. `入力動画` を選びます。複数動画も選べます。
2. `シーンフォルダ` を確認します。出力はこの中の `images/` に入ります。
3. まず `抽出間隔` を決めます。迷ったら `1.0` 秒から始めます。
4. 通常は `変化補正` をONにします。すぐ切り出したいだけなら `クイック抽出` をONにします。
5. `画像保存方法` を選びます。初回は `新規のみ追加` で十分です。
6. 実行前チェックが `準備完了` になったら `フレーム抽出を実行` を押します。
7. 完了後、Step 2で抽出結果を確認します。

Step 1は、解析用の縮小画像と、実際に保存する画像を分けて扱います。解析は `解析幅` のグレースケール画像で行い、`images/` に保存される画像は元動画の解像度を保ちます。

## 固定間隔と変化補正

### 固定間隔

`抽出間隔` は、何秒ごとに1枚を候補にするかです。30fpsの動画で `1.0` 秒なら、おおよそ30フレームごとに1枚を候補にします。

値を大きくすると枚数は減り、値を小さくすると増えます。SfMでは、まず時間方向のカバレッジが安定した固定間隔を基準にするほうが扱いやすいです。

### 変化補正

`変化補正` は固定間隔に追加する判断です。直前に採用したフレームとのペア解析を使い、次のように候補を調整します。

- 似すぎた候補は `除外: 似たフレーム` として落とす
- 固定間隔より前でも視点変化が大きい候補は `追加: 視点変化を補完` として足す
- 採用間隔が空きすぎる場合は `追加: 間隔の空きすぎを防止` として残す
- ブレ、低テクスチャ、弱い特徴点追跡の可能性がある候補はStep 2で確認しやすいように印を付ける

### クイック抽出

`クイック抽出` は解析を飛ばして、指定した `抽出間隔` でそのまま切り出すモードです。早い代わりに、変化補正やStep 2向けの確認ラベルは作りません。

内容確認用の軽い切り出しや、手元ですぐ画像だけ欲しいときに使います。Metashapeへ送る本番フレームを作る場合は、通常はOFFにして変化補正を使うほうが安全です。

## 間隔の考え方

| 設定 | 意味 | 目安 |
| --- | --- | --- |
| `抽出間隔` | 基準候補の間隔 | まず `1.0` 秒 |
| `最小` | 追加候補を入れる最小間隔 | まず `0.5` 秒 |
| `最大` | 低変化でも空きすぎない安全間隔 | まず `2.0` 秒 |

枚数が多すぎる場合は `抽出間隔` を少し上げます。逆にカメラ移動が速く、視点変化を取り逃がしていそうなら `抽出間隔` を下げるか、`最小` を小さくします。

## シーン距離

`シーン距離` は、変化補正の自動閾値の前提です。

- `近距離・歩行`: 室内、建物周り、柱、植栽、家具など近い構造物が多い撮影向けです。
- `遠景・空撮`: 空撮、広場、山、海岸など、対象物が遠く同じ移動量でも画像変化が小さく出る撮影向けです。

迷ったら、歩き撮りや建物内外は `近距離・歩行`、ドローンや広い屋外は `遠景・空撮` から始めます。

## 解析幅とJPEG品質

`解析幅` は、変化補正、ブレ確認、特徴点追跡に使う解析画像の横幅です。値を大きくすると細部を見やすくなりますが遅くなります。通常は既定値のままで十分です。

`JPEG品質` はffmpegの `-q:v` です。数値が小さいほど高画質で容量が大きくなります。既定の `2` は高品質寄りです。

## 実行後にできるもの

| 出力 | 内容 |
| --- | --- |
| `images/` | 抽出されたエクイレクタングラー静止画 |
| `selected_frames.csv` | Step 2で確認する採用/除外候補と解析情報 |
| `extract_report.json` | 抽出条件や結果の記録 |
| `extract_cache.npz` | 再解析を速くするためのキャッシュ |

Step 2では、`selected_frames.csv` の判定をGUIラベルに変換して表示します。`追加`、`除外`、`要確認` が多すぎる場合は、Step 1の間隔やシーン距離を見直します。

## よくある判断

- まずは `抽出間隔 1.0秒`, `最小 0.5秒`, `最大 2.0秒`, `変化補正 ON` で試します。
- 抽出枚数が多すぎる場合は `抽出間隔` を上げます。
- 似たフレームが多い場合は、Step 2で確認し、必要なら `抽出間隔` を上げるか `遠景・空撮` を試します。
- ブレ候補が多い場合は、撮影素材自体を確認します。Step 2で問題ないものだけ採用に戻せます。
- 同じ動画を作り直すときは `リセットして上書き` を選びます。
- `クイック抽出` は便利ですが、Step 2向けの解析ラベルが出ないため、本番用の選別には通常抽出を使います。
